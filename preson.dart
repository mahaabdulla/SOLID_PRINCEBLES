class Preson {
  int? id, height, weight;

  // methods
  changeHeight(int height) {
    if (height <= 0) print("Plz enter  positve number + ");
  } // validation to value i dont wana - values

  changeWeight() {} // validation to value i dont wana - values
  bmi() {} // cala bmi weight/h2
  saveBmi() {}
  getBmiHistory() {}
}
